// push_contentful_draft tool — implementation in Session 1e
export const pushContentfulDraftTool = {
  name: 'push_contentful_draft',
  description: 'Pushes a completed draft Field Note to Contentful as a draft entry. Never publishes. Only call this after research is complete, code is tested, and the quality gate passes.',
}
